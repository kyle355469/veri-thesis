% !TeX root = ../main.tex

\appendix{B}{Per-Module Results and Reproduction}
\label{app:modules}

\section{Per-Module Pass Counts}

Table~\ref{tab:module-appendix} lists, for every module task, the number
of passing samples over generated samples in five representative runs
(labels per Table~\ref{tab:run-inventory}). This is the tabular form of
the heatmap in Figure~\ref{fig:heatmap}; the table body is generated
directly from each run's records by
\texttt{scripts/thesis\_figures/gen\_module\_table.py}.

\begingroup
\small
\setlength{\tabcolsep}{5pt}
\begin{longtable}{lrrrrr}
  \caption[Per-module pass counts across runs]{Passing samples / generated
    samples per module task. ``--'' = task absent from the run. Columns
    straddle the June-25 S-box testbench repair
    (Section~\ref{sec:results-artifacts}); \textsc{Router-2t} postdates
    it and the other four columns predate it, so the
    \texttt{aes\_sbox}/\texttt{aes\_inv\_sbox} rows are not comparable
    across columns.}
  \label{tab:module-appendix} \\
  \toprule
  Module & \textsc{Direct-t0} & \textsc{Pipe-t0} & \textsc{Cache-off} & \textsc{Pipe-120b} & \textsc{Router-2t} \\
  \midrule
  \endfirsthead
  \toprule
  Module & \textsc{Direct-t0} & \textsc{Pipe-t0} & \textsc{Cache-off} & \textsc{Pipe-120b} & \textsc{Router-2t} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  \input{back/module_table_body}
\end{longtable}
\endgroup

\section{Reproduction Commands}

All runners live under \texttt{scripts/} in the project repository and
require a live vLLM endpoint (\texttt{VLLM\_BASE\_URL},
\texttt{VLLM\_MODEL}) plus Verilator on \texttt{PATH}. The commands below
reproduce the thesis's principal arms; output directories match
Table~\ref{tab:run-dirs}.

\begin{lstlisting}[language={},caption={Reproducing the principal
experimental arms.},label={lst:repro}]
# Headline pipeline (temp 0) and pure-model baseline
python scripts/run_agentic_plan_legacy_realbench.py \
  --task-level module --samples 1 --temperature 0 \
  --output-dir runs/agentic_plan_legacy_realbench_plan_hallu_fix_t0
python scripts/run_realbench_direct_model.py \
  --task-level module --samples 1 --temperature 0 \
  --output-dir runs/realbench_direct_model

# Repair-cache ablation (swap off | task | run)
python scripts/run_agentic_plan_legacy_realbench.py \
  --task-level module --samples 20 \
  --planner-search-mode hybrid --repair-cache task \
  --output-dir runs/..._task_rep_cache

# Grounding single-fix ablations
#   --no-planner-inject-catalog | --no-planner-ground-reuse |
#   --no-planner-completeness-gate

# Full cascade router, original v1 rule (Tier-0 LLM decider + plan probe)
python scripts/run_realbench_routed.py \
  --router cascade --decider llm --route-rule v1 --samples 10 \
  --output-dir runs/Full-2T_router_oss20B_sync6_func4

# Re-derived rule (Router-v2: pipeline-default carve-out, no probe)
python scripts/run_realbench_routed.py \
  --router cascade --decider llm --route-rule v2-20b --samples 10 \
  --output-dir runs/Full-2T_router_oss20B_syn6_func4_s10_t02_v2

# Per-module pass tables and figures
python scripts/module_pass_rate_report.py runs/<run_dir> \
  -o <run_dir>/module_pass_rate_analysis.md
cd scripts/thesis_figures && for f in fig_*.py; do python $f; done
\end{lstlisting}
